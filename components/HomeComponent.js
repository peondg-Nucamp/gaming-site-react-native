import React, { Component } from "react";
import { View } from "react-native";

class Home extends Component {
  static navigationOptions = {
    title: "All Your Base Games",
  };
  render() {
    return <View />;
  }
}

export default Home;
